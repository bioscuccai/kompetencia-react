= form_for @competence_tier_group do |f|
  - if @competence_tier_group.errors.any?
    #error_explanation
      %h2= "#{pluralize(@competence_tier_group.errors.count, "error")} prohibited this competence_tier_group from being saved:"
      %ul
        - @competence_tier_group.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :description
    = f.text_area :description
  .actions
    = f.submit 'Save'
