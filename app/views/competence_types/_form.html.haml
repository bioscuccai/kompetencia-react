= form_for @competence_type do |f|
  - if @competence_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@competence_type.errors.count, "error")} prohibited this competence_type from being saved:"
      %ul
        - @competence_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .actions
    = f.submit 'Save'
