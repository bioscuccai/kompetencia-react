'use strict';

import React from 'react';

export default React.createClass({
  render(){
    return <div>
      <form>
        
      </form>
    </div>;
  },
  
  onSubmit(e){
    e.preventDefault();
  }
});
